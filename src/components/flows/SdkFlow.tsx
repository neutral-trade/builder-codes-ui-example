export function SdkFlow() {
  return (
    <div className="flow-content">
      <p className="eyebrow">Direct integration</p>
      <h2>Deposit with the Neutral SDK</h2>
      <p>
        Build the deposit transaction in your app and pass the shared Solana
        Kit signer into the SDK transaction flow.
      </p>
      <div className="flow-placeholder">SDK deposit controls</div>
    </div>
  );
}
