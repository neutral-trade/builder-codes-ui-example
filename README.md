# builder-codes-ui-example

Copy `.env.example` to `.env.local`, then set your RPC URL and exactly one builder attribution value.
See `.env.example` for every public configuration option and its default.
